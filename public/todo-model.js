App.Model.Task = Backbone.Model.extend({

	defaults:{
		complete:false
	}
})    

App.CompleteModel.CompleteTask = Backbone.Model.extend({

})

App.IncompleteModel.IncompleteTask = Backbone.Model.extend({

})

App.StatModel.stat = Backbone.Model.extend({

})